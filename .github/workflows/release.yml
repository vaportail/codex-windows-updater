name: Release

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
      - "v[0-9]+.[0-9]+.[0-9]+-*"

permissions:
  contents: write       # create the GitHub Release
  id-token: write       # OIDC for Sigstore signing
  attestations: write   # publish the build-provenance attestation

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Build (release)
        run: cargo build --release

      - name: Stage artifact
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Copy-Item target/release/codex-launcher.exe dist/codex-launcher.exe
          $hash = (Get-FileHash dist/codex-launcher.exe -Algorithm SHA256).Hash.ToLower()
          "$hash  codex-launcher.exe" | Out-File -FilePath dist/codex-launcher.exe.sha256 -Encoding ASCII -NoNewline
          Write-Host "SHA-256: $hash"

      - name: Generate build provenance attestation
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: dist/codex-launcher.exe

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
          files: |
            dist/codex-launcher.exe
            dist/codex-launcher.exe.sha256
          body: |
            ## Verifying this build

            This release was built by GitHub Actions from tag `${{ github.ref_name }}`
            (commit `${{ github.sha }}`) and is signed via Sigstore. To verify the
            binary you downloaded matches the one this workflow produced:

            ```
            gh attestation verify codex-launcher.exe --owner vaportail
            ```

            SHA-256 of the published binary is in `codex-launcher.exe.sha256`.
